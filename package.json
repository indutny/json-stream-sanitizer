{
  "name": "json-stream-sanitizer",
  "version": "1.0.2",
  "description": "Sanitize strings in JSON stream",
  "main": "lib/sanitizer.js",
  "scripts": {
    "lint": "eslint lib test",
    "lint:fix": "npm run lint -- --fix",
    "mocha": "mocha --reporter=spec test/*-test.js",
    "test": "npm run mocha && npm run lint",
    "bench": "node benchmark/"
  },
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com/indutny/json-stream-sanitizer.git"
  },
  "keywords": [
    "JSON",
    "stream",
    "sanitizer",
    "replace",
    "regexp"
  ],
  "author": "Fedor Indutny <fedor@indutny.com>",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/indutny/json-stream-sanitizer/issues"
  },
  "homepage": "https://github.com/indutny/json-stream-sanitizer#readme",
  "devDependencies": {
    "eslint": "^6.8.0",
    "mocha": "^7.0.1"
  }
}
